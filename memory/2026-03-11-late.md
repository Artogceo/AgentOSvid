# 2026-03-11 (вечер) — Продолжение

## Проблема: Защита статуса не работает

**Что происходит:**
- Задачи переходят в `in-progress` 
- Но `pickedUp: None`, `subagentId: None`, `tz: НЕТ`
- Защита в `updateTask` не срабатывает

**Почему:**
- Возможно, статус меняется через другой endpoint (не `updateTask`)
- Или кто-то другой (не я) меняет статус
- Или защита не работает как ожидалось

**Нужно проверить:**
1. Есть ли другие endpoints для изменения статуса?
2. Работает ли защита в `updateTask`?
3. Кто/что меняет статус задач?

**Rate limit:**
- Смена модели Org на Gemini не помогла
- Лимит на `sessions_spawn`, не на модель
- Нужно реже спавнить или использовать другой подход

## Сделано:
- ✅ Org переконфигурирован на Gemini + main session
- ✅ Gateway перезапущен
- ✅ Попытка spawn Org — rate limit
- ✅ Попытка exec pickup — задача уже в in-progress

## Завтра:
1. Проверить все endpoints API
2. Усилить защиту от прямого изменения статуса
3. Найти кто меняет статус
4. Настроить правильную автоматику

**Git:** Org настроен, но автоматика не работает из-за rate limit и обхода защиты.
